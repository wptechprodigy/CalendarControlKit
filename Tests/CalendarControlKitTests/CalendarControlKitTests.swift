import XCTest
@testable import CalendarControlKit

final class CalendarControlKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CalendarControlKit().text, "Hello, World!")
    }
}
